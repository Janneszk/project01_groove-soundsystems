{
  "name": "groove",
  "version": "1.0.0",
  "description": "A project for managing music tracks.",
  "scripts": {
    "start": "parcel src/index.html",
    "build": "parcel build src/index.html",
    "serve": "parcel serve src/index.html",
    "lint": "eslint src/**/*.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jannes zur Kammer",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.12.0"
  },
  "devDependencies": {
    "eslint": "^8.0.0"
  }
}
